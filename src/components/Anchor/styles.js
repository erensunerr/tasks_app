import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    anchor: {
    },
    container: {
        // display: "flex",
        // alignSelf: "center",
    },
});

export default styles;
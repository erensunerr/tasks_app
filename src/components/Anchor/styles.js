import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    anchor: {
        color: "#4c0d80",
        fontWeight: "bold",
        fontSize: 16,
    },
    container: {
        display: "flex",
    },
});

export default styles;
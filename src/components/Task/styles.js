import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        marginTop: 8,
        alignItems: "center",
        flex: 1,
    },
    description: {
        marginLeft: 8,
        fontSize: 16,
        fontWeight: "bold",
    }
})

export default styles;